import { Paineis } from "./App";

const Interface = () => {
  return (
    <main className="flex items-center justify-center min-h-screen font-semibold">
      <Paineis />
    </main>
  );
};

export default Interface;
